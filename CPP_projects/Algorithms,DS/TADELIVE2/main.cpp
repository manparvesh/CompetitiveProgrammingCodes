#include <bits/stdc++.h>
using namespace std;

int main()
{
    int i,n,x,y;
    scanf("%d %d %d",&n,&x,&y);
    int a[n],b[n];
    for(i=0;i<n;i++) scanf("%d",&a[i]);
    for(i=0;i<n;i++) scanf("%d",&b[i]);

    return 0;
}
